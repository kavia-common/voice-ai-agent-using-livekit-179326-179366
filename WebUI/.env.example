# Environment variables for the WebUI (React CRA uses REACT_APP_ prefix for exposure to browser)
# IMPORTANT: Do not commit real secrets. These are placeholders only.
# Ask the orchestrator to set these in the deployment environment.

# LiveKit settings (used in services/livekitClient.ts)
REACT_APP_LIVEKIT_URL=
REACT_APP_LIVEKIT_API_KEY=
REACT_APP_LIVEKIT_API_SECRET=

# Note: The app will run without these, but LiveKit functionality will be no-op until set.
